# .envrc — auto-activating venv (Template A: repo-root .venv)
# First `cd` creates the venv (slow); subsequent `cd`s just activate it.
# No secrets here — safe to commit.
if ! test -d .venv; then
  python3 -m venv .venv
fi
source .venv/bin/activate
