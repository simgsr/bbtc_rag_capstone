services:
  - type: web
    name: sermon-intelligence
    runtime: docker
    plan: starter
    region: singapore
    envVars:
      - key: PORT
        value: 7860
    disk:
      name: sermon-data
      mountPath: /app/data
      sizeGB: 1
