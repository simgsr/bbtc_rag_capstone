# BBTC Sermon Intelligence — Python dependencies.
# Versions are pinned to the tested set for reproducible installs.
# Target platform: Apple Silicon (macOS) with Python >= 3.11.
#   pip install -r requirements.txt

# --- Pipeline orchestration (weekly scrape + ingest) ---
dagster==1.13.6
dagster-webserver==1.13.6

# --- Scraping & document parsing ---
cloudscraper==1.2.71
beautifulsoup4==4.14.3
PyMuPDF==1.27.2.3
python-docx==1.2.0
python-pptx==1.0.2
ebooklib==0.20

# --- Vector store & embeddings ---
chromadb==1.5.9
sentence-transformers==5.5.1

# --- LLM orchestration (agent + ingest) ---
langchain==1.3.9
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
langchain-groq==1.1.2
langchain-google-genai==4.2.3
langchain-openai==1.3.0
langgraph==1.2.9

# --- Apple Silicon (MLX) inference & embeddings ---
mlx-lm==0.31.3
mlx-embeddings==0.1.0    # optional: only needed for EMBED_BACKEND=mlx_bge / mlx_qwen

# --- UI, visualisation & config ---
gradio==6.16.0
plotly==6.7.0
python-dotenv==1.2.2

# --- Testing (`make test`) ---
pytest==9.1.1
