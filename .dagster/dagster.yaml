code_servers:
  # Raised to 1800s (30 min) so the code server survives long scrape+ingest jobs.
  # heartbeat_timeout was removed in newer Dagster; reload_timeout is the equivalent.
  reload_timeout: 1800
